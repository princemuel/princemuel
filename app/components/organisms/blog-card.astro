---
import PostImage from "@/assets/images/illustration-article.svg";
import AvatarImage from "@/assets/images/image-avatar.webp";
import { Image, Picture } from "astro:assets";
import Link from "../atoms/link.astro";

// postimage category publishdate title description authorpic authorname authorlink
// tags/category
---

<article
  class:list={[
    "group flex min-w-80 flex-col gap-6 p-6 leading-normal",
    "rounded-lg border border-gray-600",
    "shadow-[4px_4px_1px] shadow-gray-600",
  ]}
>
  <Picture
    alt="Design"
    src={PostImage}
    formats={["avif", "webp"]}
    fallbackFormat="svg"
    class="min-h-48 rounded-lg object-cover"
  />

  <header class="flex flex-col gap-4">
    <ul role="list" class="flex items-center gap-1">
      {
        [1, 2, 3].map((_) => (
          <li class="font-mon rounded bg-yellow-300 px-1.5 py-1 text-xs font-semibold text-gray-900">
            <Link href="#">Learning</Link>
          </li>
        ))
      }
    </ul>

    <p class="text-xs font-normal text-gray-900">
      Published <time datetime="">21 Dec 2023</time>
    </p>
    <h3
      class="text-xl font-extrabold text-gray-900 group-hover:text-yellow-500 focus:text-yellow-500"
    >
      <Link href="#">HTML & CSS foundations</Link>
    </h3>
    <p class="text-sm font-normal text-gray-500">
      These languages are the backbone of every website, defining structure,
      content, and presentation.
    </p>
  </header>

  <footer class="rounded-md bg-gray-100 p-2">
    <figure class="flex items-center gap-4">
      <Image
        src={AvatarImage}
        width={64}
        height={65}
        alt="Author"
        class="size-10 rounded-full"
      />
      <figcaption class="self-center text-sm font-bold text-gray-900">
        Greg Hooper
      </figcaption>
    </figure>
  </footer>
</article>
