---
import { join } from "node:path";
import { AstroFont as Font } from "astro-font";

const fontsPath = join(process.cwd(), "app", "assets", "fonts");
---

<Font
  config={[
    {
      name: "Mono Lisa",
      src: [
        {
          style: "normal",
          path: join(fontsPath, "lisa-normal.woff2"),
        },
        {
          style: "italic",
          path: join(fontsPath, "lisa-italic.woff2"),
        },
      ],
      preload: false,
      display: "swap",
      cssVariable: "--font-mono-lisa",
      fallback: "monospace",
      fallbackName: "__Mono_Lisa_Fallback",
    },
  ]}
/>
