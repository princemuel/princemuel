import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Prince Muel',
  description: '',
};

interface Props {}

const Page = (props: Props) => {
  return (
    <main className=''>
      <div></div>
    </main>
  );
};

export default Page;
